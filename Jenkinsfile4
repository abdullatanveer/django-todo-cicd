 pipeline {
    agent any

    stages {
        stage('Build') {
            agent {
                docker {
                    image 'django:latest'
                    args '-u root' // Additional arguments, if needed
                }
            }
            steps {
                // Steps to be executed inside the Docker container
                sh 'python3 manage.py'
            }
        }
    }
}
