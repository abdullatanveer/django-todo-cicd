 pipeline {
    agent {
        label 'any' // Replace 'my-agent-label' with your specific agent label or use 'any'
    }

    stages {
        stage('Build') {
            steps {
                script {
                    // Run inside the Docker container
                    docker.image('django:latest').inside {
                        sh 'python3 manage.py'
                        // Add other build steps here
                    }
                }
            }
        }
    }
}
