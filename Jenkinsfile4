 pipeline {
    agent {
        docker {
            image 'django:latest'
            args '-u root' // Add any additional arguments if needed
        }
    }

    stages {
        stage('Build') {
            steps {
                sh 'python3 manage.py'
            }
        }
    }
}
